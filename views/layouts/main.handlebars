<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LetsBlog</title>
    <link rel="stylesheet" href="./stylesheet/style.css"> 
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
</head>
<body>
  <header class="display-flex justify-space-between align-center p-2" id="navContainer">
    <nav class="navbar" id="navEdits">
            <div id="navAlign">
              <div id="title">Lets Blog!</div>
              <a class="navLinks" href="/">Home</a>
              <a class="navLinks" href="/login">Sign Up</a>
              {{#if logged_in}}
        <a href="/profile">Dashboard</a> |
        <button class="no-button" id="logout">logout</button>
              {{else}}
              <a class="navLinks" id="logIn" href="/login"> Login</a>
              {{/if}}
            </div>
        </div>
    </nav>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

</html>

